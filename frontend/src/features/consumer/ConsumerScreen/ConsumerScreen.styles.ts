import styled from 'styled-components';

export const ConsumerHomeContainer = styled.div`
  background-color: var(--main-grey);

  padding: 0 100px;
`;

export const CabinetContainer = styled.div`
  padding: 24px;
`;

export const LoanContainer = styled.div`
  padding: 0 100px;
  margin-top: 10px;
`;

export const QuestionContainer = styled.div`
  background-color: var(--primary-color);
  height: 320px;
  padding: 20px 100px 0;
`;

export const ImgContainer = styled.img`
  width: 242px;
  height: 45px;
`;

export const TitleContainer = styled.p`
  font-weight: 600;
  font-size: 24px;
  line-height: 32px;
  letter-spacing: 2px;
`;

export const SubTitleContainer = styled.p`
  font-weight: 400;
  font-size: 22px;
  line-height: 30px;
  letter-spacing: 1px;
`;

export const PageTitle = styled.h1`
  font-size: var(--page-title-size);
  line-height: var(--page-title-line-height);
  font-weight: 500;
`;

export const PageSubtitle = styled.h2`
  font-size: var(--page-subtitle-size);
  line-height: var(--page-subtitle-line-height);
  font-weight: 600;
`;

export const CabinetTitle = styled.h4`
  font-size: var(--cabinet-title-size);
  line-height: var(--page-subtitle-line-height);
  font-weight: 400;
`;
